# @when/server

Hono API server with SQLite persistence via Drizzle ORM.

## Development

```bash
pnpm dev    # starts with tsx watch on port 3456
```

Database auto-creates at `data/when.db` on first run.
