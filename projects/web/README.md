# @when/web

React client built with Vite, TanStack Router, TanStack Query, and Tosui UI.

## Development

```bash
pnpm dev    # starts Vite dev server on port 5173
```

API requests to `/api` are proxied to the server at port 3456.
